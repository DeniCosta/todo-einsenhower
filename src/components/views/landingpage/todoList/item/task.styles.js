import styled from "styled-components";

const TaskStyled = styled.div`
  display: flex;
  justify-content: space-between;
  width: 80%;
  padding: 10px;
  margin: 5px auto;
  background-color: #fff;
  border-radius: 10px;
  box-shadow: 6px 4px 10px -3px rgba(0, 0, 0, 0.59);
`;

export default TaskStyled;
